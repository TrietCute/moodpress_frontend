<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <FrameLayout
        android:id="@+id/container_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_icon_soft_circle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <ImageView
            android:id="@+id/imgMoodIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            tools:src="@drawable/ic_emotion_satisfied"
            tools:tint="@color/colorPrimary"/>
    </FrameLayout>

    <TextView
        android:id="@+id/tv_mood_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:fontFamily="sans-serif-medium"
        android:textColor="#212121"
        android:textSize="15sp"
        tools:text="Vui vẻ"
        app:layout_constraintStart_toEndOf="@id/container_icon"
        app:layout_constraintTop_toTopOf="@id/container_icon" />

    <TextView
        android:id="@+id/tvMoodCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif"
        android:textColor="#757575"
        android:textSize="14sp"
        tools:text="42% (3)"
        app:layout_constraintBaseline_toBaselineOf="@id/tv_mood_name"
        app:layout_constraintEnd_toEndOf="parent" />

    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/progressMood"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        app:trackThickness="8dp"
        app:trackCornerRadius="4dp"
        app:trackColor="#F0F0F0"
        app:indicatorColor="@color/emotion_satisfied"
        android:progress="50"
        app:layout_constraintTop_toBottomOf="@id/tv_mood_name"
        app:layout_constraintStart_toStartOf="@id/tv_mood_name"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>